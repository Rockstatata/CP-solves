#include <bits/stdc++.h>
using namespace std; 
typedef long long int ll;
const int N = 1e5 + 2, n = 9;

void solve() {
    int n;
    cin>>n;
    string s;
    cin>>s;
    cout<<s.back()<<endl;
}
int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t = 1;
     cin >> t;
    for (int i = 1; i <= t; i++) {
        solve();
    }
}